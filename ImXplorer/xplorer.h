static void HelpMarker(const char* desc);
namespace XPLORER
{
    void ShowXplorer(); //shows xplorer window
    struct Shortcut_data; //will hold the paths for the shortcut; has add_path(), remove_path(), update();
}
